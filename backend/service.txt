[Unit]
Description=Course Equivalency API
After=network.target

[Service]
User=root
WorkingDirectory=/opt/ceq/backend
EnvironmentFile=/opt/ceq/backend/.env
Environment=PATH=/opt/ceq/backend/.venv/bin
ExecStart=/opt/ceq/backend/.venv/bin/gunicorn --workers 3 --bind 127.0.0.1:5000 app:app
ExecReload=/bin/kill -s HUP $MAINPID
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target